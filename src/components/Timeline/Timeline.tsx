import Grid from '../Grid';
import Box from '../Box';

const Timeline = () => {
  return (
    <div>
      timeline
      <Grid step={2} height={'40px'} />
      <Box title={'title'} />
    </div>
  );
};

export default Timeline;
